version: "3.8"

services: # 容器
  myredis: 
    image: redis:latest
    container_name: my_redis
    command: redis-server --requirepass 123456
    ports: 
      - 6379:6379
    volumes:
      - D:/data/docker/redis/data:/data
volumes:
  data:
    driver: local